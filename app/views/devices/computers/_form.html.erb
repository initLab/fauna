<li>
  <%= form_for [:user, @computer], html: {class: 'inline-form'}, remote: true do |f| %>
    <%= f.text_field :value, required: true, pattern: '[A-Za-z]{12}' %>
    <%= f.submit "Запиши", class: 'button' %>
  <% end %>
</li>
