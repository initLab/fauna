<% if @phone.errors.any? %>
  $('#edit_phone_<%= @phone.id %>').replaceWith('<%= j render 'form' %>');
  displayFormErrors();
<% else %>
  $('#edit_phone_<%= @phone.id %>').replaceWith("<%= j render(@phone) %>");
<% end %>
